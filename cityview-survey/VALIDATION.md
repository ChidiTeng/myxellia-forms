# Validation

- `npm run build`: passed with Vite.
- Original allocation terms compared with converted JSX: exact match.
- Browser interaction tests: not completed; the environment lacked a browser runtime.

Before deployment, verify selection gating, scrolling to unlock acceptance, Back
navigation and fresh acceptance, API errors and retry, duplicate submissions,
confirmation after storage, keyboard navigation, and desktop/mobile layouts.
The backend and email service are integration points, not implemented services.
